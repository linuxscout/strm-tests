Representer sous la norme IEEE-754 32 bits le nombre suivant
\begin{arab}[utf]
مثل العدد الآتي حسب المعيار IEEE-754 32 bits
\end{arab}

\num{ {{ binary_repr }} }

\begin{itemize}
  \item \textbf{Input:} ${{ "%.4f"|format(abs_value) }} = \num{ {{ binary_repr }} }$
  \item \textbf{Normalized form:} $1.{{ mantissa }} \times 2^{{ exponent }}$
  \item \textbf{Sign bit:} {% if sign == 1 %}-{% else %}+{% endif %} $\Rightarrow$ {{ sign }}
  \item \textbf{Exponent:} ${{ exponent }} + 127 = {{ exponent_raw }} \Rightarrow {{ exponent_bits }}$
  \item \textbf{Pseudo-mantissa:} ${{ mantissa }}$
  \item \textbf{Final binary representation:}  ${{ final_binary }} $
  \item \textbf{Hexadecimal form:} $ {{ hex }} $
  \end{itemize}
