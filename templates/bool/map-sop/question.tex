{{tr("Let the function be given by its canonical form, Draw the Karnaugh table and simplify.", mode="par")}}


{% if RENDER_MODE == "question" %}
{% for data in data_list %}
    $F{{ loop.index }}(A,B,C,D) = {{ data.dnf_dict.default|normalize_formula }}$
{% endfor %}
 {% endif %}

 {% if RENDER_MODE == "answer" %}
{% for data in data_list %}
    $F{{ loop.index }}(A,B,C,D) = {{ data.dnf_dict.default|normalize_formula }}$

\begin{karnaugh-map}[4][4][1][{{ data.cd }}][{{ data.ab }}]
  \minterms{ {{ data.minterms | join(", ") }} }
  \maxterms{ {{ data.maxterms | join(", ") }} }
  {% if dontcares %}
    \terms{ {{ data.dontcares | join(", ") }} }{X}
  {% endif %}

    {{ data.simplification |format_map_terms(method=data.method.lower())|join("\n") }}

 \end{karnaugh-map}

     {{tr("Simplified Sum of products")}}  : ${{ data.sop_dict.default|normalize_formula }}$\\
     {{tr("Simplified product of sums")}}  : ${{ data.pos_dict.default|normalize_formula }}$

{% endfor %}
 {% endif %}
