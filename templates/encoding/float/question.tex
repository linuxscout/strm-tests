
{{tr("Represent under IEEE-754 32 bits standard the following number:", mode="par")}}



{{  number }}
{% if RENDER_MODE == "answer" %}
{{tr("Final binary representation")}}: ${{ binary_repr|group4 }}$

\begin{itemize}
  \item \textbf{ {{tr("Input")}} }: ${{ "%.4f"|format(abs_value) }} =  {{ binary_repr|group4 }} $
  \item \textbf{ {{tr("Normalized form")}} }: $1.{{ mantissa|group4 }} \times 2^{{ exponent }}$
  \item \textbf{ {{tr("Sign bit")}} }: {% if sign == 1 %}-{% else %}+{% endif %} $\Rightarrow$ {{ sign }}
  \item \textbf{ {{tr("Exponent")}} }: ${{ exponent }} + 127 = {{ exponent_raw }} \Rightarrow {{ exponent_bits|group4 }}$
  \item \textbf{ {{tr("Pseudo-mantissa")}} }: ${{ mantissa|group4 }}$
  \item \textbf{ {{tr("Final binary representation")}} }:  ${{ final_binary|group4 }} $
  \item \textbf{ {{tr("Hexadecimal form")}} }: $ {{ hex|group4 }} $
  \end{itemize}
{% endif %}
